const {Game} = require("../db")

module.exports = {
getGameHandler : () => {},

getIdGameHandler : () => {},

postGameHandler : () => {},

updateGameHandler : () => {},

deleteGameHandler : () => {},
}