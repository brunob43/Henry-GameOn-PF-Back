const {Doc} = require("../db")

module.exports = {
getGameHandler : () => {},

getIdGameHandler : () => {},

postGameHandler : () => {},

updateGameHandler : () => {},

deleteGameHandler : () => {},
}